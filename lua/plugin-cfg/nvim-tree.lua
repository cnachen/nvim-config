require('nvim-tree').setup {
  git = {
    enable = true
  },
  renderer = {
    icons = {
      show = {
        file = false,
        folder = false,
        folder_arrow = false
      }
    }
  }
}
